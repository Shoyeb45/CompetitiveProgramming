#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ll n, m;
    cin >> n >> m;
    ll a[n];
    for(ll i=0; i<n; i++)   cin >> a[i];     

    ll b[m];
    for(ll i=0; i<m; i++)   cin >> b[i];    
     
    return 0;
}
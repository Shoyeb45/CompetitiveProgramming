#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t;

    while (t--)
    {
        long long a, b, l;
        cin >> a >> b >> l;

        for(int x=0; ; x++) {
            for(int y=0; ; y++) {
            }
        }
    }
    
}